You are validating a candidate Problem Database entry. Produce a single Verdict. Use the rule ids listed below verbatim in `rule_violated` so the generator's retry block can address them precisely.

Candidate (and surrounding context):
{{ inputs | tojson(indent=2) }}

Validate the following rules in one pass:

1. `complexity_mismatch` — the `complexity` field must equal `inputs.target_complexity`.
2. `background_implausible` — `background` must not contradict the company profile, product list, or service organisation present in `inputs`.
3. `scenario_unrealistic` — the problem must be a plausible customer-service scenario for this company; no fantasy hardware or impossible failure modes.
4. `resolution_hints_invalid` — keys (if present) must be a subset of {"docs_request","l1","l2","l3"}; values must be short, on-topic, and consistent with the stated `complexity` (no `l3` depth on a `simple` problem and vice versa).
5. `category_off_topic` — `category` must name the actual subsystem described in `title` / `summary`; a generic catch-all is a warning, a wrong subsystem is an error.
6. `complexity_voice_mismatch` — narrative depth in `background` must match `complexity`: `simple` reads as a single symptom; `medium` includes 2–3 hypotheses; `complex` includes a detail that rules out the obvious first hypothesis.

For each rule violated, emit one Issue with `severity` ("error" for hard violations, "warning" for stylistic concerns), `location` (which field), `rule_violated` (one of the ids above, verbatim), `explanation`, and an optional `suggested_fix`.

Set `passed=true` only when there are no `error`-severity issues. Set `checker="combined_problem_check"`.
